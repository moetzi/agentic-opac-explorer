# ══════════════════════════════════════════════════════════════════════
# Agentic GraphRAG — Requirements
# Python 3.10+
#
# Install:
#   pip install -r requirements.txt
#
# Catatan torch (GPU CUDA 12.1):
#   Versi torch di sini adalah CUDA 12.1 build (cu121).
#   Jika tidak punya GPU, ganti dengan:
#       torch==2.5.1
#   Atau install dari index PyTorch CPU:
#       pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# ══════════════════════════════════════════════════════════════════════

--extra-index-url https://download.pytorch.org/whl/cu121

# ── Core: State Management & Validation ───────────────────────────────
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# ── LLM Orchestration (LangChain) ─────────────────────────────────────
langchain-core==1.3.3
langchain-openai==1.1.7
langchain-ollama==1.0.1

# ── Knowledge Graph & Vector DB ───────────────────────────────────────
neo4j==6.1.0
neo4j-graphrag==1.14.1

# ── Embedding & ML ────────────────────────────────────────────────────
sentence-transformers==5.3.0
transformers==5.5.0
huggingface-hub==1.9.2
tokenizers==0.22.2
safetensors==0.7.0
torch==2.5.1+cu121
scikit-learn==1.8.0
numpy==2.4.2
scipy==1.17.0

# ── Object Storage (MinIO / S3) ───────────────────────────────────────
boto3==1.42.83
botocore==1.42.83
minio==7.2.20

# ── Data Pipeline ─────────────────────────────────────────────────────
pandas==2.3.3
pyarrow==23.0.0
beautifulsoup4==4.14.3
lxml==6.0.2
tqdm==4.67.2

# ── HTTP & Networking ─────────────────────────────────────────────────
requests==2.32.5
httpx==0.28.1
urllib3==2.6.3

# ── User Interface ────────────────────────────────────────────────────
streamlit==1.45.1

# ── Evaluation Framework ──────────────────────────────────────────────
deepeval==4.0.5
google-generativeai==0.8.6
google-genai==1.70.0
ragas==0.2.14
datasets==3.2.0
